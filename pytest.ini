[pytest]
pythonpath = .
testpaths = tests
markers =
    api: pruebas solo de API (rápidas, sin browser)
    ui: pruebas de UI (necesitan browser)
    cross_layer: comparan capas (simulador vs API vs UI)
